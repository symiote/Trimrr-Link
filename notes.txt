->made by ritesh
dark mode at index page 23.30

we use UAparser library to detext user device type.
use React Qr Code gernator library to gernator QR code

to fetch user location we use  const response = awiat fecth("https://ipapi.co/json");
                               const {city ,country_name:country} = await response.json();

for loaction chart we use "ReCharts" library  
